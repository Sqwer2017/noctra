/**
 * Знак YouTube «play» в фирменном красном.
 *
 * Почему свой, а не из библиотеки иконок: lucide-react (как и Feather)
 * намеренно не содержит бренд-иконок — их убрали из набора. Тащить ради
 * одного знака целую библиотеку вроде `react-icons` было бы лишним весом
 * в бандле, поэтому рисуем сами.
 *
 * Форма знака повторяет официальный логотип: скруглённый прямоугольник
 * с треугольником внутри. Цвет фиксированный (`#FF0000`) — это требование
 * брендбука YouTube, знак нельзя перекрашивать в произвольные цвета.
 */
export function YouTubeGlyph({
  size = 16,
  className = "",
}: {
  size?: number;
  className?: string;
}) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      aria-hidden="true"
      className={`m-0 block shrink-0 ${className}`}
    >
      {/* Корпус знака */}
      <path
        fill="#FF0000"
        d="M23.5 6.19a3.02 3.02 0 0 0-2.12-2.14C19.5 3.55 12 3.55 12 3.55s-7.5 0-9.38.5A3.02 3.02 0 0 0 .5 6.19C0 8.08 0 12 0 12s0 3.92.5 5.81a3.02 3.02 0 0 0 2.12 2.14c1.88.5 9.38.5 9.38.5s7.5 0 9.38-.5a3.02 3.02 0 0 0 2.12-2.14C24 15.92 24 12 24 12s0-3.92-.5-5.81z"
      />
      {/* Треугольник «play» — вырезан цветом фона контейнера */}
      <path fill="#fff" d="M9.55 15.57V8.43L15.82 12l-6.27 3.57z" />
    </svg>
  );
}
